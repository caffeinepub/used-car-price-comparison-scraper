{
  "frontend/src/App.tsx": "Main app with TanStack Router, QueryClientProvider, ThemeProvider, sticky header navigation (now including \"Cross-Model Search\" link to `/cross-search`), profile setup modal, PriceAlertBanner, footer, and routes for all pages including the new `/cross-search` route rendering CrossModelSearchPage.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations; adds `useDashboardWidgets` as an alias for `useGetDashboardWidgets` (used by DashboardPage) and `useGetNearDuplicates` as an alias for `useGetDuplicateClusters` (used by DuplicateMergePage), fixing both TypeScript import errors without modifying the page files.",
  "frontend/src/pages/DashboardPage.tsx": "Full dashboard page with stat cards, My Widgets section, stale listing reminder (using correct `staleListings`/`onDismissAll` props), sortable/filterable/paginated table, inline editing (null-safe via per-row `currentEdit` variable), bulk delete, column customization (with required `onReorder` prop), and bulk archive by age dialog; `ExportFilterPanel` called without the removed `listings` prop.",
  "frontend/src/pages/AddListingPage.tsx": "Form page for manually adding a car listing with all required fields, inline validation, loading state, and success redirect; uses a local `ListingFormData` interface instead of importing `ListingData` from backend.",
  "frontend/src/pages/ComparisonPage.tsx": "Price comparison page with make/model selectors, price history line chart with price drop annotation markers, mileage-adjusted listings table, and statistics panel; `ComparisonExportFilterPanel` called without the removed `listings` prop, and `PriceDropEvent` imported from `../hooks/useQueries`.",
  "frontend/src/components/PriceStatisticsPanel.tsx": "Statistics panel showing min/max/average price, listing count, and cheapest source; now accepts an optional `dealScores` prop and renders a \"Deal Scores\" section listing each matching listing with its color-coded badge (emerald/amber/red).",
  "frontend/src/index.css": "Global styles with dark automotive OKLCH theme (default), a complete light-mode override under the `.light` selector with warm-white backgrounds and amber accents, Inter/Rajdhani fonts, card-panel utility, and comprehensive @media print rules hiding navigation and non-essential UI.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/Rajdhani fonts, custom automotive color tokens (surface, amber, emerald, steel-border, muted-text), amber glow shadow, and card shadow.",
  "frontend/src/pages/CSVImportPage.tsx": "CSV bulk import page with drag-and-drop, paste textarea, client-side parsing, row-level validation, preview table, and bulk create mutation; uses a local `ListingFormData` interface instead of importing `ListingData` from backend.",
  "frontend/src/utils/csvExport.ts": "New utility module providing `csvEscape` (special-character escaping), `buildCSV` (converts headers + row objects to a CSV string), and `downloadCSV` (triggers a browser file download via a Blob URL).",
  "frontend/src/components/ExportFilterPanel.tsx": "Dashboard export filter dialog with a new \"Saved Presets\" section at the top; users can save the current filter state as a named preset, apply/rename/delete existing presets, with all preset operations wired to the backend via React Query hooks and styled with amber accents on the dark theme.",
  "frontend/src/components/ComparisonExportFilterPanel.tsx": "Comparison export filter dialog with the same \"Saved Presets\" section as ExportFilterPanel but scoped to `presetType='comparison'`; supports save, apply, rename, and delete actions for comparison-specific filter presets with inline inputs and loading spinners.",
  "frontend/src/pages/WatchlistPage.tsx": "Watchlist page with add/remove functionality, share watchlist button that generates a token and copies a URL to clipboard; removes the non-existent `useWatchlistBestDeals` hook and the best deals summary section that depended on it.",
  "frontend/src/pages/PriceAlertsPage.tsx": "Price alerts management page with add/delete alert form and per-alert card showing matching listings; filters listings client-side from `useGetAllListings` instead of using the removed `useGetListingsBelowTargetPrice` hook, and removes the non-existent `PriceAlert` backend type import.",
  "frontend/src/pages/SavedSearchesPage.tsx": "Saved searches page with apply/rename/delete functionality; uses a local `SavedSearch` interface, and fixes the rename mutation call to use `name` instead of `newName`.",
  "frontend/src/components/PriceAlertBanner.tsx": "Global banner rendered below the header; fetches price alerts and for each shows a dismissible row when listings exist below the target price, navigating to '/' (dashboard root route) on click.",
  "frontend/src/components/OnboardingEmptyState.tsx": "Centered onboarding prompt card shown when no listings exist and no filters are active; features a car icon, headline, description, and two CTA buttons navigating to /add and /import, styled with the dark automotive amber/orange theme.",
  "frontend/src/components/PaginationControls.tsx": "Reusable pagination controls component with previous/next buttons, numbered page buttons (with ellipsis for large page counts), a mobile page indicator, a rows-per-page selector (10/25/50/100), and a row range summary; styled with the dark charcoal and amber/orange theme.",
  "frontend/src/pages/MarketOverviewPage.tsx": "Analytics page displaying Most Tracked Models, Biggest Price Drops, Best Deals, and Price Drop Listings sections — fixes imports to use the correct hook names `useGetMarketOverview` and `useGetPriceDropListings`.",
  "frontend/src/pages/DuplicateMergePage.tsx": "Page for reviewing and merging near-duplicate listing clusters; uses `any[]` for cluster typing instead of importing `ListingData`, and uses `deleteIds` (not `discardIds`) in the merge mutation call.",
  "frontend/src/hooks/useTheme.ts": "React context and hook for dark/light theme management; when authenticated, hydrates the theme from backend preferences on load (overriding localStorage) via `useUserPreferences`, and persists theme changes to the backend via `useSaveUserPreferences` alongside the current column prefs JSON from localStorage. Falls back to localStorage-only behavior for unauthenticated users.",
  "frontend/src/hooks/useColumnPreferences.ts": "React hook managing dashboard column visibility and order; when authenticated, hydrates initial state from backend preferences (taking precedence over localStorage) via `useUserPreferences`, and saves changes to the backend via `useSaveUserPreferences` alongside the current theme from localStorage. Falls back to localStorage-only behavior for unauthenticated users.",
  "frontend/src/components/ColumnCustomizationPanel.tsx": "Popover panel component for customizing dashboard table columns, rendering checkboxes for show/hide toggling, drag-and-drop reordering via HTML5 drag API, and up/down arrow buttons, with required columns (checkbox, actions) disabled.",
  "frontend/src/pages/ActivityLogPage.tsx": "Activity log page displaying reverse-chronological listing change history with action icons, affected IDs, field change details, and relative timestamps; defines `ActivityLogEntry` as a local interface instead of importing from backend.",
  "frontend/src/pages/SharedWatchlistPage.tsx": "New read-only public page that reads a `token` route param, fetches the shared watchlist via `useSharedWatchlist`, and displays entries as cards with make/model/note; shows loading skeleton, not-found, and empty states.",
  "frontend/src/components/StaleListingReminderPanel.tsx": "New collapsible amber/orange panel component that displays stale listings with their age, price, and per-row Keep/Archive actions, plus a Dismiss All button; styled consistently with the dark automotive theme.",
  "frontend/src/pages/DepreciationCurvePage.tsx": "Depreciation curve page with make/model text inputs that fetches live data via `useDepreciationCurve`; shows a loading skeleton while fetching, an empty-state message when no data is returned, and falls back to placeholder data when inputs are empty — chart and breakdown table dynamically switch between live and sample data with the dark amber/orange automotive theme.",
  "frontend/src/pages/CrossModelSearchPage.tsx": "Cross-model search page with budget/mileage inputs, quick-preset buttons, and a deal score legend; results are grouped into highlighted \"Good Deals\" (emerald), \"Fair Price\" (amber), and \"Overpriced\" (red) sections with explicit client-side price-ascending sort within each tier, a `normaliseDealScore` helper that treats missing/empty scores as 'Fair', color-coded badges, a summary chip row, and a secondary make/model text filter that preserves tier grouping."
}